// @flow
export const prop = (key: string) => (props: any) => props[key]

export const theme = (key: string) => ({ theme }: any) => theme[key]

export const propTrue = (key: string) => (props: any) => props[key] ? '&' : '2hg128dyf'