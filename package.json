{
  "name": "calc",
  "version": "0.1.0",
  "description": "在线计算能力练习",
  "private": true,
  "scripts": {
    "start": "parcel public/index.html",
    "build": "npm run clean && parcel build --no-source-maps public/index.html",
    "clean": "rm -rf dist/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "npm run build && tar zcvf calc.tgz dist/*"
  },
  "author": "",
  "license": "MIT",
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@idan-loo/expression": "^0.1.0",
    "parcel-bundler": "^1.12.0",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-router-dom": "^4.3.1",
    "styled-components": "^4.1.3"
  }
}
